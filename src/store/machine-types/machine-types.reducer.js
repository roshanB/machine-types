import { MachineTypeActionTypes } from "./machine-types.action-types";
import { } from 'react-bootstrap/com'


const INITIAL_STATE = [
  {
    id: 'machine_id_1',
    typeId: 'mtype_id',
    typeName: 'Bulldozers',
    typeTitle: 
  }
]

const machineTypeReducer = (state = INITIAL_STATE, action) => {
  <ComboBox
    switch (action.type) {
    default:
return state;
  }
};

export default machineTypeReducer;
